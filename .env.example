# Base de datos.
# En desarrollo local (npm run dev, DB corriendo con `docker compose up db`):
#   DATABASE_URL="postgresql://pedidos:pedidos@localhost:5442/pedidos"
# Corriendo la app DENTRO de docker-compose (servicio `app`), usá el nombre
# del servicio de la DB en vez de localhost:
#   DATABASE_URL="postgresql://pedidos:pedidos@db:5432/pedidos"
DATABASE_URL=""

# Auth.js — cualquier string random largo. Generalo con: openssl rand -base64 32
AUTH_SECRET=""

# Login con Google (Google Cloud Console > APIs & Services > Credentials).
# Redirect URI autorizado: https://<tu-dominio>/api/auth/callback/google
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# SMTP para el mail de confirmación de pedido. Si se dejan vacías, la app
# sigue funcionando pero no manda mails (solo loguea un warning).
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""
